Subject.py 파일에는 그래프를 출력하기 위해 필요한 데이터들을 엑셀 파일에서 불러오는 기능을 하는 함수들이 정의 되어있다.
extract_subject() / print_subject() / extract_data_man(subject) / extract_data_woman(subject) 의 4개의 함수가 정의 되어 있는데, 각각의 함수의 기능과 동작 방식은 다음과 같다.
1. extract_subject() 
csv 모듈의 기능을 이용하여 변수에 과제에 제공된 엑셀 파일의 데이터를 저장한 뒤, 과목 명이 저장된 데이터의 2번째 열의 값(과목명)을 중복을 확인 한 후 저장한다.
2. print_subject()
사용자에게 그래프를 그릴 과목명을 입력받기 위한 함수이다.
extract_subject()함수를 이용해 과목명들을 저장한 리스트에서 과목명들을 순서대로 출력한 뒤, 사용자로부터 과목명을 입력받는다. 사용자의 입력이 올바른 과목명이면 해당 과목명을 return하고 
그렇지 않으면 다시 입력받는다.
3.extract_data_man(subject)
subject 과목의 남자의 점수 분포를 추출하는 함수이다.
점수 분포를 표준점수 : 인원 의 딕셔너리 형태로 값을 저장한 뒤 값을 저장한 딕셔너리를 return한다.
4. extract_data_man(subject)
subject 과목의 여자의 점수 분포를 추출하는 함수이다.
점수 분포를 표준점수 : 인원 의 딕셔너리 형태로 값을 저장한 뒤 값을 저장한 딕셔너리를 return 한다.
